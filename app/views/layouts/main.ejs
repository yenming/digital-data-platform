<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Digital Data Platform</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- 自定義 CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Meta Tags -->
    <meta name="description" content="Digital Data Platform">
    <meta name="keywords" content="Digital Data Platform化">
    <meta name="author" content="Digital Data Platform">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= title %> - Everise Ltd">
    <meta property="og:description" content="Digital Data Platform">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= typeof req !== 'undefined' ? req.protocol + '://' + req.get('host') + req.originalUrl : '' %>">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
</head>
<body>
    <!-- 導航欄 -->
    <%- include('../partials/navbar', { user: typeof user !== 'undefined' ? user : null, page: typeof page !== 'undefined' ? page : null }) %>
    
    <!-- 主要內容 -->
    <main role="main">

        <!-- 頁面內容 -->
        <%- typeof body !== 'undefined' ? body : '' %>
    </main>
    
    <!-- 頁腳 -->
    <%- include('../partials/footer') %>
    
    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- 自定義 JavaScript -->
    <script src="/js/main.js"></script>
    
    <!-- 頁面特定 JavaScript -->
    <% if (typeof scripts !== 'undefined') { %>
        <% scripts.forEach(script => { %>
            <script src="<%= script %>"></script>
        <% }); %>
    <% } %>
</body>
</html>
